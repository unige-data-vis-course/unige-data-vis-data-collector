#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

echo "Running unit tests (pytest)..."

# Run only the tests directory to keep it fast
python3 -m pytest -q tests

echo "All tests passed."
